# Courseflow

A focused student assignment planner built with plain HTML, CSS, and JavaScript. It includes a responsive weekly calendar, sorted assignment list, course color coding, and a form that back-plans study time from each assignment's due date and estimated effort.

Open `index.html` directly in a browser, or serve the folder with any static file server.